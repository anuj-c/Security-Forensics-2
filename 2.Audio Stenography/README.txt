To run the encoding part run the following command:
python3 encode.py

Enter the key for the encoding
Enter the message that should be hidden in the audio file

The encoded audio file will be saved in the same directory as the original audio file.

To run the decoding part run the following command:
python3 decode.py

Enter the key for the decoding

The decoded message will be printed on the terminal.